"""
Records written out in full: the model, the builders, and the page.

`card.py` says what a written-out record *is*, `builders.py` turns each
kind of record into one, and `paper.py` puts it on A4. The screen half
lives in `dialogs/record_card_dialog.py`, because it is a dialog.
"""
